pipeline {
  agent { docker { image 'python:3.14.5-alpine3.23' } }
  tools {
    maven 'Maven 3.3.9'
  }
  stages {
    stage("Build") {
      steps {
         sh "python3 --version"
       }
    }
  }
}
